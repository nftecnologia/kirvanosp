allOf:
  - $ref: '#/components/schemas/account_detail'
  - type: object
    properties:
      latest_kirvano_version:
        type: string
        description: Latest version of Kirvano available
        example: "3.0.0"
      subscribed_features:
        type: array
        items:
          type: string
        description: List of subscribed enterprise features (if enterprise edition is enabled)